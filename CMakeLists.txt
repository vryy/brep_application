message("")
message("**** configuring KratosBRepApplication ****")

if(${PYTHON_VERSION_MAJOR} MATCHES "2")
    message("KratosBRepApplication is compiled with Python 2")
    include(${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists2.txt)
elseif(${PYTHON_VERSION_MAJOR} MATCHES "3")
    message("KratosBRepApplication is compiled with Python 3")
    include(${CMAKE_CURRENT_SOURCE_DIR}/CMakeLists3.txt)
else()
    message("KratosBRepApplication does not detect the Python version")
endif()

message("**** finish configuring KratosBRepApplication ****")
message("")
